<%= provide(:flash, 'no') %>

<div class="home-wrap">
  <%= image_tag "logo.png" %>
  <% flash.each do |type, msg| %>
    <div class="pure-alert pure-alert-<%= type %>"><%= msg %></div>
  <% end %>
  <div class="home-boards">
    <h4>Boards</h4>
    <ul>
      <% @boards.each do |board| %>
        <li><%= link_to board.title, board, class: "red-link" %></li>
      <% end %>
    </ul>
  </div>
  <% if logged_in? %>
    <%= link_to "out!", mod_logout_path, method: :delete %>
  <% end %>
</div>
